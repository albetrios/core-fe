{
  "_comment": "Intentional doc references the staleness scanner (pnpm docs:staleness) should NOT flag. Paths resolve against GIT-TRACKED files (deterministic across machines + CI), so `paths` = tokens with no tracked file/dir on purpose: illustrative example paths, cross-repo (core-be) paths, and gitignored per-developer local files (mcp.json, settings.local.json). `symbols` = identifiers that intentionally do not exist as a src/ export yet (planned APIs, prose examples). Each entry needs a reason; the scanner is advisory (weekly sync-drift canary) so this list absorbs deliberate references, not real drift. Keep it shrinking toward zero.",
  "paths": [
    {
      "value": "docs/american-spelling-organization",
      "reason": "Illustrative Git branch name in the branch-naming tables in CLAUDE.md, agent-os/rules/fe-git-branch-naming.mdc, and docs/process/trunk-based-workflow.md; not a filesystem path."
    },
    {
      "value": "docs/branch-naming-guidance",
      "reason": "Illustrative Git branch name in the git branch -m and git push commands in CLAUDE.md; not a filesystem path."
    },
    {
      "value": ".claude/rules",
      "reason": "AGENTS.md documents this symlink is intentionally absent (Claude Code follows CLAUDE.md)."
    },
    {
      "value": ".claude/.cursor/.codex",
      "reason": "Prose shorthand for the three real config dirs .claude/ .cursor/ .codex/ in agent-os/evals/README.md."
    },
    {
      "value": "docs/routes.txt",
      "reason": "core-be path listed in a cross-repo requirement table (unified-auth-otp-requirement.md)."
    },
    {
      "value": "src/shared/utils/http/response.util.ts",
      "reason": "core-be path referenced in backend-list-pagination-spec.md (backend conventions)."
    },
    {
      "value": "src/infrastructure/payment/stripe.client.ts",
      "reason": "core-be path referenced in backend-list-pagination-spec.md (backend conventions)."
    },
    {
      "value": "src/pages/notifications",
      "reason": "Illustrative example page (e.g. /notifications) in fe-requirement-format SKILL.md."
    },
    {
      "value": "agent-os/mcp/mcp.json",
      "reason": "Gitignored per-developer MCP config, scaffolded by pnpm mcp:setup (.gitignore L79); docs reference it intentionally."
    },
    {
      "value": ".claude/settings.local.json",
      "reason": "Gitignored per-developer Claude Code local settings (.gitignore L82); docs reference it intentionally."
    },
    {
      "value": "src/lib/__tmp-probe.tsx",
      "reason": "Throwaway file inside a worked example in guard-authoring/SKILL.md — the snippet creates it, runs the gate against it, then rm -f's it. Never tracked, by design."
    }
  ],
  "symbols": [
    {
      "value": "useFeatureFlag",
      "reason": "Planned PostHog provider; posthog-frontend.md marks it not-yet-implemented."
    },
    {
      "value": "useNow",
      "reason": "Illustrative hypothetical hook name ('useNow-style') in 2026-06-29-codebase-audit-loop.md."
    }
  ]
}
